# SS14 Resources

Use this when editing `Resources/`.

- Load `ss14-ecs-prototypes`, `ss14-localization-strings`, `ss14-prototypes-locale`.
- Also load `ss14-graphics-generic-visualizer-appearance` for `Appearance` or `GenericVisualizer` work.
- Use the most specific existing subtree.
- Run YAML or RSI validation appropriate to the touched files.
