# SS14 Tests

Use this when editing `Content.Tests/` or `Content.IntegrationTests/`.

- Load `ss14-tests-authoring`.
- Pick the smallest test layer that actually covers the risk.
- Keep fixtures local and avoid turning a content test into an integration test without need.
- Call out what still needs in-game verification if tests cannot cover it.
